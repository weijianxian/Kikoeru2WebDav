{
  "name": "cf-webdav-remote-raw",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/worker.js && node test/run-tests.mjs",
    "test": "node test/run-tests.mjs",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
